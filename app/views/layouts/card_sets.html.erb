<% content_for :sub_nav_list_items do %>
    <% if user_signed_in? %>
        <%= link_to 'new set', new_card_set_path %> /
    <% end %>
    <%= link_to 'list sets', card_sets_path %>
    <% if user_signed_in? %>
        / <%= link_to 'new card', new_card_path %>
        / <%= link_to 'list cards', cards_path %>
    <% end %>
    <%= yield :extra_items %>
<% end %>

<%= render :file => 'layouts/application' %>